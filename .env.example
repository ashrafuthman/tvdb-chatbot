# Copy this file to .env and fill in your credentials.
# Never commit real secrets to source control.

# OpenAI API key with access to the selected model.
OPENAI_API_KEY=your-openai-api-key

# Override if you want a specific model variant (defaults to gpt-4o-mini-2024-07-18).
OPENAI_MODEL=gpt-4o-mini-2024-07-18

# TVDB API credentials.
TVDB_API_KEY=your-tvdb-api-key
TVDB_PIN=your-tvdb-pin

# Optional: change if you need a custom base URL (defaults to https://api4.thetvdb.com/v4).
# TVDB_BASE_URL=https://api4.thetvdb.com/v4
